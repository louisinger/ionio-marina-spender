export const infos = {
  CONSTRUCTOR_INPUTS:
    'Parameters passed to the contract before the script generation. Identified by "$" prefix in the function scripts.',
  COVENANT_BRANCHES: 'Paths to unlock coin locked by the contract.',
  INPUT_STACK: 'The stack expected by the script as input.',
  REQUIREMENTS:
    'The conditions describing the transaction that can unlock the contract. Enforced by the script thanks to introspection opcodes.',
  SCRIPT: 'Tapscript of the branch.',
};
